CREATE TABLE TRADE (
                       id BIGINT AUTO_INCREMENT PRIMARY KEY,
                       type VARCHAR(255),
                       quantity INT,
                       price DOUBLE
);
